{
	"name": "basic-authorization-header",
	"private": true,
	"version": "0.0.0",
	"description": "Create Authorization HTTP headers. Compatible with `Headers()`, `Request()`, and `fetch()`.",
	"keywords": ["http", "Basic Auth", "Authorization", "Header"],
	"type": "module",
	"main": "index.js",
	"types": "index.d.ts",
	"scripts": {
		"format": "biome format . --write",
		"lint": "oxlint . && biome lint .",
		"test": "npm run lint && node --test",
		"tsc": "tsc index.js --allowJs --declaration --emitDeclarationOnly --target es2021",
		"minify": "esbuild index.js --minify --outfile=index.min.js"
	},
	"author": "Andrew Pyle",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/andrew-pyle/basic-authorization-header"
	},
	"devDependencies": {
		"@biomejs/biome": "1.3.1",
		"esbuild": "^0.19.5",
		"np": "^8.0.4",
		"oxlint": "^0.0.14",
		"typescript": "^5.2.2"
	}
}
