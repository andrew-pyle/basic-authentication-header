{
	"name": "basic-authentication-header",
	"private": true,
	"version": "0.0.0",
	"description": "Create Authorization HTTP headers. Compatible with `Headers()` and `fetch()`.",
	"type": "module",
	"main": "index.js",
	"scripts": {
		"lint": "oxlint . && biome lint .",
		"format": "biome format . --write",
		"test": "npm run lint && node --test",
		"tsc": "tsc index.js --allowJs --declaration --emitDeclarationOnly --target es2021"
	},
	"author": "Andrew Pyle",
	"license": "ISC",
	"devDependencies": {
		"@biomejs/biome": "1.3.1",
		"np": "^8.0.4",
		"oxlint": "^0.0.14",
		"typescript": "^5.2.2"
	}
}
