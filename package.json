{
	"name": "basicauth",
	"private": true,
	"version": "0.1.0",
	"description": "Helpers to create Authorization HTTP headers. Compatible with `Headers()` and `fetch()`.",
	"type": "module",
	"main": "index.js",
	"scripts": {
		"lint": "oxlint . && biome lint .",
		"format": "biome format . --write",
		"test": "node --test"
	},
	"author": "Andrew Pyle",
	"license": "ISC",
	"devDependencies": {
		"@biomejs/biome": "1.3.1",
		"@types/node": "^20.8.8",
		"oxidation-compiler": "^0.2.3",
		"oxlint": "^0.0.14"
	}
}
